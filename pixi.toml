[workspace]
name = "clio-store"
description = "FastAPI server for managing connectomics/EM datasets"
channels = ["conda-forge"]
platforms = ["osx-arm64", "osx-64", "linux-64"]

[dependencies]
python = "3.12.*"
pip = "*"

# Production dependencies (direct imports only; transitive deps resolved by pixi)
[feature.prod.pypi-dependencies]
aiohttp = ">=3.11.0"
fastapi = ">=0.115.0"
google-auth = ">=2.28.0"
google-cloud-bigquery = ">=3.17.0"
google-cloud-firestore = ">=2.15.0"
google-cloud-storage = ">=2.14.0"
httpx = ">=0.27.0"
hypercorn = ">=0.17.0"
pydantic = ">=1.10.18,<2"
python-multipart = ">=0.0.9"
pyjwt = ">=2.8.0"
requests = ">=2.32.0"
uvicorn = ">=0.30.0"

# Development dependencies
[feature.dev.pypi-dependencies]
pytest = ">=8.0"

# Environments
[environments]
default = { features = ["prod", "dev"], solve-group = "default" }
prod = { features = ["prod"], solve-group = "default" }

[tasks]
dev = "uvicorn main:app --reload --host 0.0.0.0 --port 8080"
deploy = { cmd = "python scripts/deploy.py", description = "Deploy to Cloud Run" }
